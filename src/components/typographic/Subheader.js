import React from 'react';

const Subheader = ({ children }) => {

    return (
        <div className="subheader-text">
            { children }
        </div>
    );
}

export default Subheader;